function GithubCard({ title, value }) {

    return (

        <div className="github-card">

            <h2>{value}</h2>

            <p>{title}</p>

        </div>

    );

}

export default GithubCard;